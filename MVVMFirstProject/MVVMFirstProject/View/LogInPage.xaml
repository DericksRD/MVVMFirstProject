<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MVVMFirstProject.View.LogInPage">
    <ContentPage.Content>
        <StackLayout VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"
                     WidthRequest="300">
            <Image Source="LogoIntec.png" HeightRequest="140" Margin="0, 15"/>
            <Entry Placeholder="Email" Text="{Binding user.Email}"/>
            <Entry Placeholder="Password" Text="{Binding user.Password}" 
                   IsPassword="True"/>
            <Button Text="LogIn" BackgroundColor="Red" TextColor="White" 
                    CornerRadius="10" Command="{Binding LogInCommand}"/>
            <Label HorizontalOptions="CenterAndExpand" FontSize="18" Margin="0, 15">
                <Label.FormattedText>
                    <FormattedString>
                        <Span Text="Registro" 
                              TextColor="Red"
                              TextDecorations="Underline"/>
                    </FormattedString>
                </Label.FormattedText>
            </Label>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>